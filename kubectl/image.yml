name: kubectl
# renovate: datasource=github-releases depName=kubernetes/kubernetes versioning=semver extractVersion=^v?(?<version>.*)$
version: 1.37.0
# renovate: datasource=github-releases depName=mikefarah/yq versioning=semver extractVersion=^v?(?<version>.*)$
yq_version: 4.53.6
description: Ubuntu minimal container with kubectl and yq for Kubernetes operations
category:
  - ubuntu
  - kubernetes
  - tools
registry: guidedtraffic
maintainer: guided-traffic.com
platforms:
  - linux/amd64
  - linux/arm64
